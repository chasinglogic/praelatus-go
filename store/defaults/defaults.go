// Package defaults provides defaults for stores, for example it will hold
// functions for creating a default Agile Project, Scrum Project, etc. as well
// as defaults for multiple models (issuetypes, labels, etc.)
package defaults
